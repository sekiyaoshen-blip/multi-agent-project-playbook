interface:
  display_name: "Project Agent Operating Model"
  short_description: "Route visible tasks with adaptive model selection"
  default_prompt: "Use $project-agent-operating-model to install or slim a native-first project operating model with long-lived visible module tasks and dispatch-time model/Thinking routing. 中文：使用 $project-agent-operating-model 安装或精简以长期显性模块任务和派单时动态 model/Thinking 路由为核心的 Native-first 项目运行模型。"

policy:
  allow_implicit_invocation: false
